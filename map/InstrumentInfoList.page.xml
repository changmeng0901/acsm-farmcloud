<?xml version="1.0" encoding="UTF-8"?>
<page xmlns="http://jboss.com/products/seam/pages"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://jboss.com/products/seam/pages http://jboss.com/products/seam/pages-2.2.xsd"
       login-required="true">

   <param name="firstResult" value="#{instrumentInfoList.firstResult}"/>
   <param name="sort" value="#{instrumentInfoList.orderColumn}"/>
   <param name="dir" value="#{instrumentInfoList.orderDirection}"/>
   <param name="logic" value="#{instrumentInfoList.restrictionLogicOperator}"/>

   <param name="from"/>
   <param name="name" value="#{instrumentInfoList.instrumentInfo.name}"/>
   <param name="buyPerson" value="#{instrumentInfoList.instrumentInfo.buyPerson}"/>
   <param name="model" value="#{instrumentInfoList.instrumentInfo.model}"/>
   <param name="insertUser" value="#{instrumentInfoList.instrumentInfo.insertUser}"/>
   <param name="updateUser" value="#{instrumentInfoList.instrumentInfo.updateUser}"/>
   <param name="baseId" value="#{instrumentInfoHome.baseId}"/>
   

</page>
